import { Point } from "./types";

export const startShape = (point: Point) => {
  // TODO
};

export const appendLine = (point: Point) => {
  // TODO
};

export const finishShape = (point: Point) => {
  // TODO
};

export const undo = () => {
  console.log("undo");
};

export const redo = () => {
  console.log("redo");
};
